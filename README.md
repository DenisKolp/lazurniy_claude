# 🏘️ Lazurny Bot - Бот для управления КП "Лазурный"

Полнофункциональный Telegram-бот для управления ассоциацией коттеджного поселка с системой верификации пользователей, голосованиями, календарем событий и админ-панелью.

## ✨ Ключевые особенности

- 🔐 **Четырехэтапная верификация** - ФИО, номер телефона, документы, адрес участка
- 👥 **Разделение прав** - обычные пользователи и администраторы
- 🗳️ **Система голосований**:
  - Пользователи могут предлагать вопросы на голосование
  - Модерация вопросов администраторами (публикация/отклонение)
  - Просмотр активных голосований и истории с результатами
  - Автоматическое закрытие и подсчет результатов с кворумом
- 📄 **Google Sheets интеграция**:
  - Экспорт результатов голосований в таблицы
  - Реестр верифицированных членов ассоциации с автообновлением
- 🔔 **Умные уведомления** - с тихими часами и персональными настройками
- ⏰ **Автоматические напоминания** - за 24 часа до событий и окончания голосований
- 🌍 **Часовые пояса** - корректная работа с Europe/Moscow (или любым другим)
- 🐳 **Docker-ready** - простое развертывание через Docker Compose
- 📊 **PostgreSQL или SQLite** - выбор базы данных на ваше усмотрение
- ⚡ **100% асинхронный** - высокая производительность с asyncio

## 📋 Возможности

### Для членов ассоциации:
- ✅ **Верификация** - четырехэтапный процесс:
  - Ввод полного ФИО (Фамилия Имя Отчество)
  - Подтверждение номера телефона
  - Загрузка документов о праве собственности
  - Указание адреса участка
- 🗳️ **Голосования**:
  - Предложение своих вопросов для голосования
  - Просмотр активных голосований и участие в них
  - История завершенных голосований с результатами
  - Отслеживание статуса своих предложений (на модерации, опубликовано, отклонено)
- 📊 **Мои вопросы** - просмотр всех созданных голосований и предложений
- 📅 **Календарь событий** - просмотр предстоящих мероприятий КП
- 📝 **Обращения в ИГ** - создание и отслеживание обращений
- 🔔 **Умные уведомления** - автоматические оповещения с тихими часами (22:00-08:00)
- ⚙️ **Настройки** - управление уведомлениями

### Для администраторов:
- 👥 **Модерация пользователей**:
  - Просмотр заявок на верификацию с полной информацией:
    - ФИО, @username, Telegram ID
    - Номер телефона, адрес участка
    - Прикрепленные документы
  - Одобрение/отклонение с указанием причины
  - Автоматический экспорт в Google Sheets при одобрении
  - Отзыв верификации у существующих членов
  - Разделение на "На проверке" и "Члены ассоциации"
- 🗳️ **Управление голосованиями**:
  - Создание голосований напрямую (сразу активное)
  - Модерация предложенных пользователями вопросов:
    - Просмотр черновиков (DRAFT)
    - Публикация вопроса (→ ACTIVE с уведомлениями всем)
    - Отклонение вопроса (→ CANCELLED с уведомлением автору)
  - Удаление активных голосований с уведомлениями
  - Автоматическое закрытие по истечении срока
  - Подсчет результатов и рассылка членам ассоциации
- 📄 **Google Sheets интеграция**:
  - Автоматический экспорт результатов голосований
  - **Реестр членов ассоциации** с автообновлением:
    - ФИО, Username, Телефон, Адрес, Дата верификации
    - Обновляется при каждом одобрении новых членов
    - Красивое форматирование с зебра-стрипингом
  - Форматированные таблицы с процентами и статистикой
  - Публичные ссылки только для чтения
- 📅 **Управление событиями**:
  - Создание событий с указанием даты, времени и места
  - Автоматические напоминания за 24 часа
  - Корректная работа с часовыми поясами (Europe/Moscow)
- 📝 **Обработка обращений**:
  - Просмотр открытых обращений (новые и в работе)
  - Ответы на обращения с уведомлением пользователя
  - Закрытие обращений
- 🚨 **Оповещения** - массовая рассылка всем членам ассоциации
- 📊 **Статистика** - общая информация о пользователях, голосованиях, событиях

## 🛠️ Технологический стек

- **Python 3.11+** - основной язык программирования
- **python-telegram-bot 20.7** - асинхронный фреймворк для Telegram ботов
- **SQLAlchemy 2.0** - современный ORM с async поддержкой
- **PostgreSQL 15** / **SQLite** - база данных (выбор на ваше усмотрение)
- **asyncpg** / **aiosqlite** - асинхронные драйверы БД
- **APScheduler 3.10** - планировщик задач для напоминаний
- **gspread 5.12** - интеграция с Google Sheets API
- **pytz** - работа с часовыми поясами
- **phonenumbers** - валидация номеров телефонов
- **Docker** + **Docker Compose** - контейнеризация

## 📦 Установка

### Предварительные требования

- Python 3.11 или выше
- pip (менеджер пакетов Python)
- Git (опционально)

### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd lazurny_bot
```

### Шаг 2: Создание виртуального окружения

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/MacOS
python3 -m venv venv
source venv/bin/activate
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 4: Настройка переменных окружения

Скопируйте файл `.env.example` в `.env`:

```bash
# Windows
copy .env.example .env

# Linux/MacOS
cp .env.example .env
```

Отредактируйте `.env` и заполните необходимые параметры:

```env
# Telegram Bot Token (получите у @BotFather)
BOT_TOKEN=your_bot_token_here

# ID администраторов (через запятую)
ADMIN_IDS=123456789,987654321

# База данных (по умолчанию SQLite)
DATABASE_URL=sqlite+aiosqlite:///./lazurny_bot.db

# Настройки голосований
VOTE_DURATION_DAYS=7
DEFAULT_QUORUM_PERCENT=50

# Настройки уведомлений
REMINDER_HOURS_BEFORE=24
QUIET_HOURS_START=22:00
QUIET_HOURS_END=08:00

# Часовой пояс
TIMEZONE=Europe/Moscow

# Режим отладки
DEBUG=False
```

### Шаг 5: Получение Bot Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в файл `.env`

### Шаг 6: Получение Admin ID

1. Откройте Telegram и найдите [@userinfobot](https://t.me/userinfobot)
2. Отправьте боту любое сообщение
3. Скопируйте ваш ID в файл `.env` (параметр `ADMIN_IDS`)

### Шаг 7: Запуск бота

```bash
python bot.py
```

Бот запустится и будет готов к работе!

## 🐳 Развертывание с Docker

### Шаг 1: Установка Docker

Установите [Docker](https://docs.docker.com/get-docker/) и [Docker Compose](https://docs.docker.com/compose/install/).

### Шаг 2: Настройка переменных окружения

Создайте файл `.env` как описано выше.

### Шаг 3: Сборка и запуск

```bash
# Сборка образа
docker-compose build

# Запуск в фоновом режиме
docker-compose up -d

# Просмотр логов
docker-compose logs -f bot

# Остановка
docker-compose down
```

### Использование PostgreSQL (опционально)

Раскомментируйте секцию `postgres` в `docker-compose.yml` и измените `DATABASE_URL`:

```env
DATABASE_URL=postgresql+asyncpg://lazurny_user:your_password@postgres:5432/lazurny_bot
POSTGRES_PASSWORD=your_password
```

## 🖥️ Развертывание на VPS

### Шаг 1: Подключение к серверу

```bash
ssh user@your-server-ip
```

### Шаг 2: Установка зависимостей

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Python и pip
sudo apt install python3.11 python3-pip python3-venv git -y
```

### Шаг 3: Клонирование и настройка

```bash
cd /opt
sudo git clone <repository-url> lazurny_bot
cd lazurny_bot
sudo python3 -m venv venv
sudo venv/bin/pip install -r requirements.txt
```

### Шаг 4: Настройка .env

```bash
sudo nano .env
# Заполните необходимые параметры
```

### Шаг 5: Создание systemd service

```bash
sudo nano /etc/systemd/system/lazurny-bot.service
```

Добавьте следующее содержимое:

```ini
[Unit]
Description=Lazurny Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/lazurny_bot
Environment="PATH=/opt/lazurny_bot/venv/bin"
ExecStart=/opt/lazurny_bot/venv/bin/python /opt/lazurny_bot/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### Шаг 6: Запуск сервиса

```bash
# Перезагрузка systemd
sudo systemctl daemon-reload

# Запуск бота
sudo systemctl start lazurny-bot

# Добавление в автозагрузку
sudo systemctl enable lazurny-bot

# Проверка статуса
sudo systemctl status lazurny-bot

# Просмотр логов
sudo journalctl -u lazurny-bot -f
```

## 🚀 Развертывание на Railway

### Шаг 1: Создание аккаунта

1. Зарегистрируйтесь на [Railway.app](https://railway.app/)
2. Подключите GitHub аккаунт

### Шаг 2: Создание проекта

1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Выберите репозиторий с ботом

### Шаг 3: Настройка переменных окружения

В разделе "Variables" добавьте:

```
BOT_TOKEN=your_token
ADMIN_IDS=123456789
DATABASE_URL=sqlite+aiosqlite:///./data/lazurny_bot.db
TIMEZONE=Europe/Moscow
```

### Шаг 4: Настройка PostgreSQL (рекомендуется)

1. Добавьте новый сервис "PostgreSQL"
2. Railway автоматически создаст переменную `DATABASE_URL`
3. Измените её на формат asyncpg:
   ```
   postgresql+asyncpg://user:password@host:port/database
   ```

### Шаг 5: Deploy

Railway автоматически развернет приложение при каждом push в GitHub.

## 🔧 Настройка и использование

### Команды бота

**Для всех пользователей:**
- `/start` - Начало работы с ботом / возврат в главное меню
- `/verify` - Начать процесс верификации
- `/voting` - Просмотр активных голосований
- `/events` - Календарь событий

**Для администраторов:**
- `/admin` - Открыть админ-панель

### Меню бота

После верификации пользователю доступны кнопки главного меню:
- 🗳️ **Голосования** - просмотр, участие в голосованиях, мои вопросы
- 📅 **События** - календарь предстоящих мероприятий
- 📝 **Обращение в ИГ** - создание и отслеживание обращений
- ⚙️ **Настройки** - включение/отключение уведомлений
- 👨‍💼 **Админ-панель** - для администраторов

### Процесс верификации

**Шаг 1: ФИО**
- Пользователь вводит полное ФИО (Фамилия Имя Отчество)
- Валидация: минимум 2 слова, 3-100 символов
- Пример: Иванов Иван Иванович

**Шаг 2: Номер телефона**
- Пользователь нажимает кнопку "📱 Отправить номер телефона"
- Telegram автоматически передает номер боту
- Бот валидирует номер с помощью библиотеки phonenumbers

**Шаг 3: Документы**
- Загрузка документов, подтверждающих право собственности
- Поддерживаемые форматы: PDF, JPG, JPEG, PNG, DOC, DOCX
- Можно загрузить несколько документов

**Шаг 4: Адрес участка**
- Ввод адреса в формате: "173", "173/1", "Лазурная 173", "Лазурная 173/1"
- Автоматическая валидация и нормализация адреса

**Шаг 5: Модерация**
- Администратор получает уведомление о новой заявке с полными данными:
  - ФИО, @username, телефон, адрес
- Просматривает документы прямо в Telegram
- Одобряет или отклоняет с указанием причины
- При одобрении данные автоматически экспортируются в Google Sheets реестр
- Пользователь получает кнопку "🏠 Старт" после одобрения

### Создание голосования (администраторы)

1. Откройте **Админ-панель** → **Голосования** → **Создать голосование**
2. Заполните поля:
   - **Название** - краткое описание вопроса (5-500 символов)
   - **Описание** - подробная информация (10-4000 символов)
   - **Варианты ответов** - от 2 до 10 вариантов (по одному на строку)
   - **Длительность** - количество дней до завершения
3. После создания:
   - Голосование автоматически становится активным
   - Все члены ассоциации получают уведомление
   - При завершении результаты экспортируются в Google Sheets (если настроено)

### Google Sheets интеграция

**Настройка (опционально):**
1. Следуйте инструкциям в [GOOGLE_SHEETS_SETUP.md](GOOGLE_SHEETS_SETUP.md)
2. Создайте Service Account в Google Cloud Console
3. Поместите `credentials.json` в корень проекта
4. Перезапустите бота

**Возможности:**
- Автоматический экспорт результатов завершенных голосований
- Форматированные таблицы с процентами и графиками
- Публичные ссылки (только чтение) в уведомлениях
- Если credentials не настроены - бот работает без экспорта

### Управление пользователями (администраторы)

**Модерация заявок:**
- **Админ-панель** → **Пользователи** → **На проверке**
- Просмотр документов, телефона, адреса
- Одобрение - пользователь становится членом ассоциации
- Отклонение - с обязательным указанием причины

**Управление членами:**
- **Админ-панель** → **Пользователи** → **Члены ассоциации**
- Просмотр списка всех верифицированных пользователей
- Возможность отозвать верификацию (пользователь вернется в статус PENDING)
- При отзыве пользователь получает уведомление

### Система уведомлений

**Автоматические уведомления отправляются о:**
- 📢 Новых голосованиях - сразу после создания
- ⏰ Предстоящих событиях - за 24 часа (в окне 23-25 часов)
- 📊 Завершении голосований - с результатами и ссылкой на Google Sheets
- ⏳ Напоминании о голосовании - за 24 часа до окончания (только тем, кто не голосовал)
- 💬 Ответах на обращения - когда администратор отвечает
- 📝 Закрытии обращений - финальный статус

**Тихие часы:**
- Настраиваются в `.env`: `QUIET_HOURS_START=22:00`, `QUIET_HOURS_END=08:00`
- В это время уведомления не отправляются
- Проверка работает по часовому поясу из `TIMEZONE`

**Управление:**
- Каждый пользователь может отключить уведомления в **Настройках**
- Администраторы всегда получают системные уведомления

## 📊 База данных

### SQLite (по умолчанию)

Простая файловая база данных, не требует установки:

```env
DATABASE_URL=sqlite+aiosqlite:///./lazurny_bot.db
```

### PostgreSQL (для продакшена)

Рекомендуется для production:

```env
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/lazurny_bot
```

### Миграции

Для изменения структуры БД можно использовать Alembic:

```bash
# Инициализация
alembic init alembic

# Создание миграции
alembic revision --autogenerate -m "description"

# Применение миграций
alembic upgrade head
```

## 🔐 Безопасность

### Рекомендации:

1. **Не храните `.env` в Git** - файл добавлен в `.gitignore`
2. **Используйте сильные пароли** для PostgreSQL
3. **Ограничьте доступ** к серверу (firewall, SSH keys)
4. **Регулярно обновляйте** зависимости
5. **Делайте резервные копии** базы данных

### Backup базы данных

```bash
# SQLite
cp lazurny_bot.db lazurny_bot_backup_$(date +%Y%m%d).db

# PostgreSQL
pg_dump -U lazurny_user lazurny_bot > backup_$(date +%Y%m%d).sql
```

## 🐛 Troubleshooting

### Бот не запускается

1. Проверьте правильность BOT_TOKEN
2. Убедитесь, что все зависимости установлены
3. Проверьте логи: `journalctl -u lazurny-bot -n 50`

### База данных не создается

1. Проверьте права доступа к директории
2. Убедитесь, что DATABASE_URL указан правильно
3. Для SQLite проверьте наличие директории `data/`

### Ошибки при верификации

1. Проверьте, что ADMIN_IDS указаны правильно
2. Убедитесь, что формат номера телефона корректен
3. Проверьте размер загружаемых файлов (макс. 20 МБ)

### Уведомления не приходят

1. Проверьте настройку QUIET_HOURS
2. Убедитесь, что пользователь не отключил уведомления
3. Проверьте, что scheduled jobs запущены

## 📝 Структура проекта

```
lazurny_bot/
├── bot.py                          # Главный файл бота
├── config.py                       # Конфигурация из .env
├── requirements.txt                # Python зависимости
├── .env                           # Переменные окружения (не в Git)
├── credentials.json               # Google Sheets credentials (не в Git)
│
├── database/
│   ├── models.py                  # SQLAlchemy модели (User, Voting, Event, Ticket)
│   ├── crud.py                    # CRUD операции для всех моделей
│   └── session.py                 # Управление сессиями БД
│
├── handlers/
│   ├── start.py                   # /start, верификация, главное меню
│   ├── voting.py                  # Голосования, просмотр, создание
│   ├── events.py                  # События, календарь
│   ├── tickets.py                 # Обращения в ИГ
│   └── admin.py                   # Админ-панель, модерация
│
├── services/
│   ├── reminder_service.py        # Напоминания о событиях и голосованиях
│   ├── notification_service.py    # Отправка уведомлений
│   ├── analytics_service.py       # Сбор статистики
│   └── sheets_service.py          # Google Sheets экспорт
│
├── utils/
│   ├── helpers.py                 # Форматирование дат, тихие часы
│   └── validators.py              # Валидация телефонов, адресов, документов
│
├── alembic/                       # Миграции БД (опционально)
├── Dockerfile                     # Docker образ
├── docker-compose.yml             # Docker Compose (bot + postgres)
│
└── docs/
    ├── FIRST_RUN.md              # Первый запуск
    ├── ARCHITECTURE.md           # Архитектура системы
    ├── TESTING.md                # Тестирование
    └── GOOGLE_SHEETS_SETUP.md    # Настройка Google Sheets
```

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте раздел Troubleshooting
2. Изучите логи бота
3. Создайте issue в GitHub репозитории

## 📄 Лицензия

MIT License

## 👨‍💻 Разработка

Для внесения изменений:

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

---

**Сделано с ❤️ для жителей КП "Лазурный"**
