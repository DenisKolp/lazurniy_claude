# ⚡ Quick Start Guide - Lazurny Bot

Быстрый старт для запуска бота за 5 минут!

## 🚀 Самый быстрый способ (локально)

### 1. Установка

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd lazurny_bot

# Создайте виртуальное окружение
python -m venv venv

# Активируйте (Windows)
venv\Scripts\activate

# Или (Linux/Mac)
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка

```bash
# Скопируйте файл примера
cp .env.example .env

# Отредактируйте .env
notepad .env  # Windows
nano .env     # Linux/Mac
```

**Минимальная конфигурация:**
```env
BOT_TOKEN=YOUR_BOT_TOKEN_FROM_BOTFATHER
ADMIN_IDS=YOUR_TELEGRAM_ID
```

### 3. Запуск

```bash
python bot.py
```

Готово! Бот запущен и работает! 🎉

---

## 🐳 С Docker (рекомендуется для продакшена)

### 1. Создайте .env

```bash
cp .env.example .env
# Отредактируйте .env и добавьте BOT_TOKEN и ADMIN_IDS
```

### 2. Запустите

```bash
docker-compose up -d
```

### 3. Проверьте логи

```bash
docker-compose logs -f bot
```

Готово! Бот работает в фоновом режиме! 🎉

---

## 📱 Получение необходимых данных

### Получить BOT_TOKEN

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/botfather)
3. Отправьте `/newbot`
4. Следуйте инструкциям
5. Скопируйте токен (выглядит как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Получить ADMIN_ID

**Способ 1:**
1. Найдите [@userinfobot](https://t.me/userinfobot)
2. Отправьте любое сообщение
3. Скопируйте ваш ID

**Способ 2:**
1. Найдите [@getmyid_bot](https://t.me/getmyid_bot)
2. Отправьте `/start`
3. Скопируйте Your user ID

---

## 🎮 Первое использование

### Для обычного пользователя:

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Отправьте `/verify` для верификации
4. Следуйте инструкциям:
   - Отправьте номер телефона
   - Загрузите документы
   - Укажите адрес

### Для администратора:

1. Отправьте `/start`
2. Пройдите верификацию (даже админу нужна верификация)
3. После верификации появится кнопка "👨‍💼 Админ-панель"
4. Или используйте команду `/admin`

---

## 📝 Основные команды

```
/start    - Начало работы
/verify   - Начать верификацию
/admin    - Админ-панель (только для администраторов)
```

---

## 🔍 Проверка работы

### Проверьте, что бот работает:

```bash
# Проверьте процесс
ps aux | grep bot.py

# Проверьте логи
tail -f logs/bot.log

# Проверьте базу данных
ls -lh lazurny_bot.db
```

### С Docker:

```bash
# Проверьте статус
docker-compose ps

# Проверьте логи
docker-compose logs bot

# Перезапустите при необходимости
docker-compose restart bot
```

---

## ⚙️ Расширенная настройка

### Настройка PostgreSQL вместо SQLite

```env
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/lazurny_bot
```

### Настройка часового пояса

```env
TIMEZONE=Europe/Moscow
```

### Настройка тихих часов

```env
QUIET_HOURS_START=22:00
QUIET_HOURS_END=08:00
```

### Настройка голосований

```env
VOTE_DURATION_DAYS=7
DEFAULT_QUORUM_PERCENT=50
```

---

## 🛠️ Типичные проблемы

### Проблема: "Invalid token"
**Решение:** Проверьте правильность BOT_TOKEN в .env

### Проблема: "Bot doesn't respond"
**Решение:**
- Проверьте, что бот запущен
- Проверьте интернет-соединение
- Проверьте логи на наличие ошибок

### Проблема: "Database locked"
**Решение:**
- Закройте все соединения к БД
- Перезапустите бота
- Или используйте PostgreSQL

### Проблема: "Admin panel not available"
**Решение:**
- Убедитесь, что ваш ID в ADMIN_IDS
- Пройдите верификацию
- Перезапустите бота

---

## 📊 Мониторинг

### Просмотр статуса бота

```bash
# Локально
ps aux | grep bot.py

# С systemd
sudo systemctl status lazurny-bot

# С Docker
docker-compose ps
```

### Просмотр логов

```bash
# Локально
tail -f logs/bot.log

# С systemd
sudo journalctl -u lazurny-bot -f

# С Docker
docker-compose logs -f bot
```

---

## 🎯 Следующие шаги

После успешного запуска:

1. ✅ **Проверьте работу** - создайте тестовое голосование
2. ✅ **Настройте backup** - регулярное резервное копирование БД
3. ✅ **Добавьте мониторинг** - настройте оповещения о сбоях
4. ✅ **Изучите документацию** - прочитайте [README.md](README.md)
5. ✅ **Протестируйте** - используйте [TESTING.md](TESTING.md)

---

## 📚 Дополнительные ресурсы

- [Полная документация](README.md)
- [Руководство по тестированию](TESTING.md)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [python-telegram-bot docs](https://docs.python-telegram-bot.org/)

---

## 💡 Советы

1. **Используйте screen/tmux** для запуска бота в фоне на VPS
2. **Настройте systemd service** для автозапуска
3. **Делайте резервные копии** базы данных ежедневно
4. **Мониторьте логи** на наличие ошибок
5. **Обновляйте зависимости** регулярно

---

**Готово! Ваш бот работает!** 🚀

Если возникли проблемы, проверьте [README.md](README.md) или [TESTING.md](TESTING.md).
