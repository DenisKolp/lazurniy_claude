# 🏗️ Архитектура Lazurny Bot

## Обзор системы

Lazurny Bot - это асинхронный Telegram-бот, построенный на современных принципах разработки с использованием чистой архитектуры и разделения ответственности.

## Структура проекта

```
lazurny_bot/
├── bot.py                          # Точка входа приложения
├── config.py                       # Конфигурация и переменные окружения
├── database/                       # Слой данных
│   ├── models.py                  # SQLAlchemy модели (ORM)
│   ├── crud.py                    # CRUD операции
│   └── session.py                 # Управление сессиями БД
├── handlers/                       # Обработчики команд и сообщений
│   ├── start.py                   # Регистрация и верификация
│   ├── voting.py                  # Голосования
│   ├── events.py                  # События
│   ├── tickets.py                 # Обращения
│   └── admin.py                   # Админ-панель
├── services/                       # Бизнес-логика и сервисы
│   ├── reminder_service.py        # Напоминания
│   ├── notification_service.py    # Уведомления
│   └── analytics_service.py       # Аналитика
└── utils/                          # Вспомогательные функции
    ├── helpers.py                 # Общие утилиты
    └── validators.py              # Валидация данных
```

## Компоненты системы

### 1. Entry Point (`bot.py`)

Главный файл приложения, отвечающий за:
- Инициализацию приложения
- Регистрацию обработчиков
- Настройку планировщика задач
- Запуск бота

**Ключевые функции:**
```python
main()              # Основная функция запуска
post_init()         # Инициализация после создания приложения
error_handler()     # Глобальный обработчик ошибок
```

### 2. Configuration (`config.py`)

Централизованное управление конфигурацией через переменные окружения.

**Параметры:**
- `BOT_TOKEN` - токен Telegram бота
- `ADMIN_IDS` - список ID администраторов
- `DATABASE_URL` - строка подключения к БД
- Настройки голосований, уведомлений и т.д.

### 3. Database Layer

#### Models (`database/models.py`)

SQLAlchemy ORM модели для основных сущностей:

**User** - Пользователи системы
- Telegram профиль
- Статус верификации (pending/verified/rejected)
- Права администратора
- Настройки уведомлений

**Voting** - Голосования
- Метаданные (название, описание)
- Варианты ответов (JSON)
- Статус (draft/active/completed/cancelled)
- Временные рамки
- Результаты

**Vote** - Голоса пользователей
- Связь пользователь-голосование
- Выбранный вариант

**Event** - События
- Информация о событии
- Дата и место проведения
- Статус отправки напоминаний

**Ticket** - Обращения
- Описание проблемы
- Вложения (JSON array)
- Статус (new/in_progress/answered/closed)
- Ответ администратора

**Notification** - Уведомления
- Целевая аудитория
- Содержимое
- Статус отправки
- Планирование

#### CRUD Operations (`database/crud.py`)

Класс-репозитории для каждой модели:
- `UserCRUD` - операции с пользователями
- `VotingCRUD` - операции с голосованиями
- `VoteCRUD` - операции с голосами
- `EventCRUD` - операции с событиями
- `TicketCRUD` - операции с обращениями
- `NotificationCRUD` - операции с уведомлениями

**Паттерн:** Repository Pattern для изоляции логики работы с БД

#### Session Management (`database/session.py`)

Управление асинхронными сессиями SQLAlchemy:
```python
async_session_maker   # Фабрика сессий
init_db()            # Инициализация БД
get_session()        # Получение сессии (dependency injection)
```

### 4. Handlers Layer

Обработчики команд и взаимодействий пользователя.

#### Start Handler (`handlers/start.py`)

**Функции:**
- Приветствие новых пользователей
- Процесс верификации (ConversationHandler)
- Главное меню для членов ассоциации

**Conversation States:**
1. `PHONE_NUMBER` - получение номера телефона
2. `DOCUMENTS` - загрузка документов
3. `ADDRESS` - указание адреса

#### Voting Handler (`handlers/voting.py`)

**Функции:**
- Просмотр активных голосований
- Участие в голосовании
- Создание голосований (админы)
- Отображение результатов в реальном времени

**Conversation States:**
1. `VOTING_TITLE` - название
2. `VOTING_DESCRIPTION` - описание
3. `VOTING_OPTIONS` - варианты ответов
4. `VOTING_DURATION` - длительность

#### Events Handler (`handlers/events.py`)

**Функции:**
- Календарь событий
- Создание событий (админы)
- Редактирование/удаление событий

**Conversation States:**
1. `EVENT_TITLE` - название
2. `EVENT_DESCRIPTION` - описание
3. `EVENT_DATE` - дата и время
4. `EVENT_LOCATION` - место проведения

#### Tickets Handler (`handlers/tickets.py`)

**Функции:**
- Создание обращений
- Просмотр статуса
- Получение ответов

**Conversation States:**
1. `TICKET_TITLE` - краткое описание
2. `TICKET_DESCRIPTION` - подробное описание
3. `TICKET_ATTACHMENTS` - вложения

#### Admin Handler (`handlers/admin.py`)

**Функции:**
- Панель управления
- Модерация пользователей
- Управление обращениями
- Экстренные оповещения
- Статистика

### 5. Services Layer

Бизнес-логика и фоновые задачи.

#### Reminder Service (`services/reminder_service.py`)

**Задачи:**
- Напоминания о событиях (за 24 часа)
- Напоминания о голосованиях
- Автоматическое закрытие истекших голосований
- Отправка результатов голосований

**Периодичность:** Каждый час

#### Notification Service (`services/notification_service.py`)

**Задачи:**
- Обработка очереди уведомлений
- Массовая рассылка
- Учет "тихих часов"
- Rate limiting

**Периодичность:** Каждые 5 минут

#### Analytics Service (`services/analytics_service.py`)

**Задачи:**
- Статистика пользователей
- Статистика голосований
- Анализ активности
- Топ активных пользователей

### 6. Utils Layer

Вспомогательные функции и утилиты.

#### Helpers (`utils/helpers.py`)

- `format_datetime()` - форматирование дат с учетом часового пояса
- `is_quiet_hours()` - проверка тихих часов
- `escape_markdown()` - экранирование для MarkdownV2
- `get_user_display_name()` - получение отображаемого имени
- `calculate_quorum()` - расчет кворума
- `format_voting_results()` - форматирование результатов

#### Validators (`utils/validators.py`)

- `validate_phone_number()` - валидация номера телефона
- `validate_document()` - проверка формата файла
- `validate_title()` - валидация названий
- `validate_description()` - валидация описаний
- `validate_voting_options()` - валидация вариантов голосования

## Потоки данных

### Регистрация пользователя

```
User -> /start -> StartHandler
  -> /verify -> ConversationHandler
  -> Phone -> Documents -> Address
  -> Create User (status=PENDING)
  -> Notify Admins
  -> Admin approves -> Update User (status=VERIFIED)
  -> Notify User
```

### Создание и участие в голосовании

```
Admin -> Create Voting -> VotingCRUD.create()
  -> Notify all users
User -> View Voting -> VotingHandler
  -> Cast Vote -> VoteCRUD.create()
  -> Update results -> VotingCRUD.update()
Time expires -> ReminderService
  -> Close voting -> Calculate results
  -> Notify all users
```

### Обработка обращения

```
User -> Create Ticket -> TicketCRUD.create()
  -> Notify Admins
Admin -> View Ticket -> Respond
  -> Update Ticket (status=ANSWERED)
  -> Notify User
```

## Асинхронность

Весь код построен на asyncio:
- `async/await` для всех операций I/O
- `AsyncSession` для работы с БД
- `python-telegram-bot` с async support
- `APScheduler` для фоновых задач

## База данных

### Схема связей

```
User 1:N Vote
User 1:N Ticket
User 1:N Voting (as creator)
Voting 1:N Vote
User 1:N Notification (optional)
```

### Индексы

- `User.telegram_id` - уникальный индекс
- `Vote(user_id, voting_id)` - составной индекс
- `Voting.status` - для быстрого поиска активных

## Планировщик задач

**APScheduler** с двумя основными задачами:

1. **Reminder Job** (каждый час):
   - Проверка событий
   - Проверка голосований
   - Закрытие истекших голосований

2. **Notification Job** (каждые 5 минут):
   - Обработка очереди уведомлений
   - Отправка с учетом тихих часов

## Обработка ошибок

### Уровни обработки:

1. **Handler level** - try/catch в обработчиках
2. **Global error handler** - перехват всех необработанных ошибок
3. **Logging** - запись всех ошибок в лог

### Стратегия восстановления:

- Автоматический перезапуск (systemd/docker)
- Сохранение состояния в БД
- Graceful shutdown

## Безопасность

### Меры защиты:

1. **Верификация** - доступ только проверенным пользователям
2. **Admin checks** - проверка прав на критичные операции
3. **Input validation** - валидация всех входных данных
4. **SQL Injection protection** - использование ORM
5. **Rate limiting** - ограничение частоты запросов

## Масштабируемость

### Текущие ограничения:

- Один процесс бота
- SQLite (для малых нагрузок)

### Возможности масштабирования:

1. **PostgreSQL** - для больших нагрузок
2. **Redis** - кэширование, очереди
3. **Load balancing** - несколько инстансов бота
4. **Message queue** - RabbitMQ/Kafka для обработки

## Мониторинг

### Метрики:

- Время отклика бота
- Количество активных пользователей
- Количество ошибок
- Размер БД
- Использование ресурсов

### Логирование:

```python
logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.INFO
)
```

## Deployment

### Варианты развертывания:

1. **Локальный запуск** - для разработки
2. **VPS + systemd** - для продакшена
3. **Docker** - контейнеризация
4. **Railway/Heroku** - PaaS платформы

## Зависимости

### Основные:

- `python-telegram-bot` - Telegram Bot API
- `sqlalchemy` - ORM
- `asyncio` - асинхронность
- `apscheduler` - планировщик

### Дополнительные:

- `phonenumbers` - валидация телефонов
- `python-dateutil` - работа с датами
- `pytz` - часовые пояса
- `pydantic` - валидация данных

## Best Practices

1. **Async everywhere** - полностью асинхронный код
2. **Repository pattern** - изоляция логики БД
3. **Conversation handlers** - для многошаговых диалогов
4. **Error handling** - на всех уровнях
5. **Logging** - детальное логирование
6. **Configuration** - через environment variables
7. **Documentation** - docstrings для всех функций

## Будущие улучшения

- [ ] Добавить unit tests
- [ ] Миграции с Alembic
- [ ] Redis для кэширования
- [ ] Метрики с Prometheus
- [ ] Webhook вместо polling
- [ ] i18n (мультиязычность)
- [ ] API для интеграций

---

**Архитектура спроектирована для надежности, расширяемости и удобства поддержки.**
